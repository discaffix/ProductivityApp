<Page
    x:Class="ProductivityApp.AppTesting.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:ProductivityApp.AppTesting.Views"

    Style="{StaticResource PageStyle}"
    mc:Ignorable="d" Loaded="Page_LoadedAsync">


    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            x:Uid="Main_Title"
            Style="{StaticResource PageTitleStyle}" />
        <Grid
            Grid.Row="1" 
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--
                The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.
            -->
            <StackPanel>
                <TextBox Text="{x:Bind ViewModel.SessionDescription}" />
                <ListView x:Name="Sessions" ItemsSource="{x:Bind ViewModel.Sessions}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Description}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <StackPanel
                    Orientation="Horizontal">
                    <Button Content="Start Session" Margin="0,5,0,0" Command="{x:Bind ViewModel.StartSession}"/>
                    <Button Content="Stop Session" Margin="15,5,0,0" Command="{x:Bind ViewModel.StopSession}"/>
                    <TextBlock Text="00:05:25" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="25,0,0,0" />
                </StackPanel>

            </StackPanel>
        </Grid>
    </Grid>
</Page>
